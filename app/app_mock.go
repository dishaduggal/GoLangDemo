package app

//MockCtx will have all mocks of dependencies
func MockCtx(failConfig ...int) (err error) {

	// Init mock MongoDB
	//Ctx.DB = mockMdb()

	// Init mock config

	// Init hhtp handler
	return
}
